version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
        - cp src/index.html dist/apples/index.html
  artifacts:
    baseDirectory: dist/apples
    files:
      - '**/*'
