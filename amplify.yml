version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
        - cp src/index.html dist/apples/index.html
        - cp -r src/assets dist/apples/
  artifacts:
    baseDirectory: dist/apples
    files:
      - '**/*'
